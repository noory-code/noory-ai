---
id: R-00000044
work_item: W-00000046
---

# R-00000044 에스컬레이션 출구·한계 config 회고

## Work

자율 실행 드라이버의 에스컬레이션 출구와 한계 설정을 구현했다. `escalate_work.py`는 열린 항목(active/review)을 `blocked`로 전환하고, 이유를 담은 pending decision을 생성해 항목의 `decision_refs`에 연결한다(불변식: 상한·막힘 도달 = `blocked` + 결정, 절대 `completed` 아님). `stage_paths.load_limits_config`는 settings.json의 선택적 `limits` 섹션(항목당 시도 상한·전역 반복·벽시계 상한)을 fail-closed로 읽는다. 실제 시도 카운트·무진전 감지·전역 예산 집행은 드라이버(W-47) 몫으로 남겼다. 실행은 Codex, 리뷰는 Claude + 독립 `claude -p` 패스(둘 다 승인).

## Decision points

- DE-00000022: W-46 경계 = 에스컬레이션 출구 + 한계 config(리더). 런타임 카운트·감지·집행은 W-47. DE-00000017의 출구·설정 부분.

## Principles applied

- SoC / SRP — "포기 시 무엇을(출구)"과 "언제 포기(루프 집행)"를 분리.
- AHA — 드라이버 없는 런타임 상태 스키마를 미리 만들지 않음.
- Fail Fast / No partial completion — `blocked` 불변식; 한계 config는 malformed 시 fail-closed.
- SSOT — 한계 값은 settings.json 단일 위치.
- DRY — lifecycle 스크립트(close_work/archive_work)와 review-config 리졸버 패턴 재사용.

## Context that helped

- DE-00000022 계약이 경계(출구+config만)를 명확히 고정.
- 기존 lifecycle 스크립트·review-config 리더가 구조와 fail-closed 계약을 제공.

## Context that was missing

- 없음(문서 브리프 명시로 Codex가 SCHEMA_V4.md 갱신).

## Next changes

- 안정화 폴링(75s 창)이 Codex의 단계 간 멈춤(테스트→구현→문서)에 속아 조기 "안정"으로 오판했다. 이번엔 테스트 통과로 실제 완성을 재확인해 넘어갔다. 향후: 안정화 창을 늘리거나 "테스트 통과 + 예상 산출물 존재"를 완료 신호로 병용.
- W-47은 `load_limits_config`의 반환에서 error 문자열을 반드시 검사해야 한다(absent와 malformed가 모두 `None`이므로 error 무시 시 조용히 한계 skip — 독립 리뷰 지적).

## Promotion decision

approved — DE-00000022와 일치, 테스트·감사 통과, 독립 claude 리뷰 승인. 상위 official 승격은 드라이버 전체 검증 후 W-00000042에서 일괄.
