---
id: W-00000221
title: 워크트리에서 실행자 환경이 깨지는 원인을 잰다
kind: fix
venue: codex
milestone:
autonomous: false
acceptance:
  - "python3 -m unittest discover -s stage/scripts/tests -p test_drive_parallel.py -q"
status: archived
terminal_disposition: rejected
verification: pending
retrospective: completed
retrospective_ref: R-00000218
promotion: not_applicable
review: not_required
scope: stage/scripts/drive_parallel.py, stage/scripts/tests/test_drive_parallel.py, stage/CHANGELOG.md
promotes:
decision_refs:
---

# W-00000221 워크트리에서 실행자 환경이 깨지는 원인을 잰다

## Purpose

클로드 실행자의 명령 승인 대기가 워크트리에서 재현되는 원인을 모르는 채로는 고칠 자리를 정할 수 없으므로, 본 체크아웃과 워크트리의 실행 조건 차이를 재서 원인을 밝힌다

## Source

O-00000028 과 W-00000214 실행 보고: 클로드 실행자(`claude -p --permission-mode acceptEdits`)가
워크트리 세션에서 `python3` 실행 승인 대기에 걸렸다. 비대화형이라 승인할 사람이 없어 인수
명령을 못 돌렸다. 본 체크아웃에서는 같은 명령이 돈다. 코덱스의 설정 실패는 2026-08-06
실행에서 재현되지 않았다(플러그인 캐시가 최신이면 안 깨지는 것으로 보이나 확인 안 됨).

## User value

깨지는 원인을 알아야 준비 단계가 짐작이 아니라 측정 위에서 고쳐진다.

## Scope

### Included

- 본 체크아웃과 워크트리에서 같은 실행 조건을 나란히 재서 차이를 밝힌다. 후보 가설(검증
  대상이지 결론이 아님): 워크트리에 없는 미추적 파일 — 예: `.claude/settings.local.json`
  허용 목록 — , 디렉터리 신뢰 상태, 환경 변수 차이.
- 재현 명령과 결과를 실행 보고(`Decisions made:`/`What changed:` 및 공유 로그)에 남긴다.
  `.stage/` 관측 기록은 만들지 않는다 — 그건 감독 세션이 옮긴다.

### Excluded

- 고치는 것은 다음 액션(W-00000222) 몫이다. 이 액션은 재기만 한다.

## Dependencies

없음. 이 스토리의 첫 액션이다.

## Risks

- 이 측정 자체가 워크트리 안에서 돈다. 코덱스 실행자의 환경에서 클로드 실행자의 조건을 재는
  것이므로, 무엇을 직접 쟀고 무엇을 재현 못 했는지 갈라 적어야 한다.

## Success criteria

- 본 체크아웃에서는 되고 워크트리에서는 안 되는 조건의 차이가 재현 명령과 함께 실행 보고에 남는다

## Next action

본 체크아웃과 워크트리의 `.claude/` 유무·내용, 환경 변수, `claude -p` 최소 재현(무해한
`python3 -V` 실행 요청)을 나란히 돌려 차이를 표로 만든다.

## Related truth

- O-00000028 — 실측 원문.


## Progress

측정은 됐고 결론이 카드의 전제를 뒤집었다. 실행자(코덱스)의 정적 대조는 정확했다 — 클로드
실행자 명령에 `--allowedTools` 없음, 본 체크아웃 허용 목록은 `Bash(git check-ignore *)`
하나뿐, 워크트리 `.claude/` 에는 추적 파일뿐. 동적 측정은 계측기 고장으로 빈 결과였다
(코덱스 샌드박스 안에서 `claude -p` 가 시작 자체를 못 함 — 판정자가 밝힘). 병합 뒤 감독
세션이 샌드박스 밖에서 같은 탐침을 두 자리에 돌렸다: **본 체크아웃도 워크트리도 똑같이
막힌다**(BLOCKED, permission approval required). 차이는 없다.

## Verification

판정 반려가 맞았다 — 지목한 원인은 두 환경에 똑같이 적용되므로 비대칭을 설명하지 못하고,
동적 실험은 샌드박스를 쟀다. 판정자 소견 처분: "다음 측정은 샌드박스 밖에서" → **수용**,
감독 세션이 그대로 수행해 대칭을 확인했다. Progress 의 "확인된 원인" 단정을 근거 강도에
맞게 낮추라 → **수용**, 이 카드에는 확인된 사실만 남겼다.

## Retrospective

R-00000218 참조.

## Promotion decision

not_applicable — 결정 기록 없음. 카드 자체는 물림: 성공 기준이 요구한 "두 환경의 차이"가
존재하지 않아 기준을 만족시킬 수 없다. 알아낸 것은 O-00000028 갱신이 소유한다.
