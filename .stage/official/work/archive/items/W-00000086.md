---
id: W-00000086
title: 리뷰 지적 처리 단계를 스킬 문서에 적는다
kind: documentation
venue: claude
parent:
priority:
autonomous: false
acceptance: []
status: archived
terminal_disposition: accepted
verification: passed
retrospective: completed
retrospective_ref: R-00000085
promotion: not_applicable
scope: stage/skills/stage-retrospective/, stage/skills/stage-drive/, stage/.claude-plugin/plugin.json, stage/.codex-plugin/plugin.json, stage/CHANGELOG.md
promotes:
decision_refs:
---

# W-00000086 리뷰 지적 처리 단계를 스킬 문서에 적는다

## Purpose

DE-00000032 가 정한 지적 처리 단계(지적마다 받는다/안 받는다/미룬다 + 근거 한 줄을 카드의
`## Verification` 에 남긴다)는 지금 결정 기록에만 있다. 스킬이 그 단계를 안 물으면 사람도
에이전트도 잊는다. 확인을 맡은 스킬에 그 단계를 적는다.

## Source

DE-00000032 (W-00000084 의 설계 결정).

## User value

리뷰 지적을 무비판적으로 다 받다가 없던 결함을 만들던 흐름이, 스킬이 매번 묻는 절차로
막힌다. 안 받은 지적에도 근거가 남아 나중에 같은 지적이 또 와도 다시 따질 필요가 없다.

## Scope

### Included

- `stage-retrospective` 스킬: 회고 확인 항목에 더한다 — 이 작업에 리뷰 판정이 있었으면,
  지적마다 처리(받는다/안 받는다/미룬다)와 근거가 카드의 `## Verification` 에 적혀 있는지
  확인한다. 판단 물음도 함께 적는다: 그 상황이 이 프로젝트에 실제로 오나. 온다면 고치는
  일이 오는 상황을 깨뜨릴 위험은 없나. P1 이어도 안 받을 수 있다는 것을 명시한다.
- `stage-drive` 스킬: 리뷰 뒤에 이 처리 단계가 있다는 사실과, 차단(P1)은 리뷰어의 목소리를
  사람 앞에 세우는 장치일 뿐 받을지는 사람이 정한다는 것을 적는다.

### Excluded

- 드라이버 코드와 리뷰 프롬프트 변경 — W-00000085.
- 훅으로 강제하는 일 — DE-00000032 가 배제했다. 안 지켜지는 것이 관찰되면 그때 다시 결정한다.

## Dependencies

- DE-00000032 (decided). W-00000085 와는 독립이라 순서 제약이 없다.

## Risks

- 확인 항목이 길어지면 회고가 무거워져 안 쓰게 된다. 항목 하나, 물음 둘을 넘기지 않는다.

## Success criteria

- `stage-retrospective` 가 리뷰 판정이 있었던 작업에 지적별 처리 기록을 확인 항목으로
  요구한다.
- `stage-drive` 가 리뷰 뒤 처리 단계의 존재와 "P1 도 사람이 정한다" 를 말한다.
- 두 `plugin.json` 의 version 이 올라 있고 `stage/CHANGELOG.md` 에 항목이 있다.
- 저장소 산출물은 영어다.

## Verification

### 기준 판정 — DE-00000032 방식 (2026-07-26)

- `stage-retrospective` 요구 — 채움. Required confirmations 에 항목이 생겼다: 리뷰 판정이
  있었으면 지적마다 받는다/안 받는다/미룬다 + 근거 한 줄이 카드 `## Verification` 에 있어야
  한다. 판단 물음 둘(그 상황이 오나 / 고치면 오는 상황을 깨뜨리나)과 "P1 도 근거를 남기고
  안 받을 수 있다", 안 받는 쪽도 기록이 남는 이유까지 함께 적었다.
- `stage-drive` — 채움. BLOCK 은 리뷰어의 목소리를 사람 앞에 세울 뿐 결정하지 않는다는 것,
  처리 단계의 존재, 기준 밖 관찰은 차단 없이 같은 처리로 들어간다는 것을 적었다.
- 버전 0.43.12, CHANGELOG 항목 — 채움.
- 산출물 영어 — 채움.

### 그 밖에 본 것

- `stage-drive` 의 `NO-PROGRESS` 문단이 낡아 틀린 사실을 말하고 있었다 — 처리: 받았다
  (위 Progress 절에 근거). 지금 동작(HEAD 대비 지문, 무변경 실패 규칙)에 맞게 바로잡았다.

### Executed at close — 2026-07-26

```
$ python3 -m unittest discover -s stage/scripts/tests -q
[exit 0]
... (25 earlier lines omitted)
Unattended run on isolated branch: stage/driver/W-00000001-1785040511 (base: main)
[W-00000002] completed on stage/driver/W-00000001-1785040511
Outcome: blocked — parent aggregation-close failed: W-00000001: parent close failed: boom; handoff on stage/driver/W-00000001-1785040511
Recommended next action: attempt cap reached / no progress / global limit exceeded → escalate_work
Unattended run on isolated branch: stage/driver/W-00000001-1785040512 (base: main)
[W-00000002] completed on stage/driver/W-00000001-1785040512
[W-00000003] completed on stage/driver/W-00000001-1785040512
Unattended run finished: 2 item(s) closed on isolated branch stage/driver/W-00000001-1785040512. Human review + merge required; the base branch was not modified.
Unattended run on isolated branch: stage/driver/W-00000001-1785040513 (base: main)
Outcome: blocked — cannot commit pre-close lifecycle for W-00000002: simulated lifecycle commit failure
Recommended next action: attempt cap reached / no progress / global limit exceeded → escalate_work
Outcome: blocked — unattended mode requires a `limits` config (absent is not unlimited here); refusing to run
Recommended next action: attempt cap reached / no progress / global limit exceeded → escalate_work
Unattended run on isolated branch: stage/driver/W-00000001-1785040513 (base: main)
Unattended run finished: 0 item(s) closed on isolated branch stage/driver/W-00000001-1785040513. Human review + merge required; the base branch was not modified.
Preflight passed. Close every other agent/editor window before continuing; the schema-v4 maintenance marker now denies concurrent Stage writes.
  unchanged operations/verification.md (unchanged)
  delete backlog B-00000001-realized.md (realized by W-00000009; git history keeps the file)
  convert backlog B-00000002-open.md -> W-00000001.md (planned work card)
  convert backlog B-00000003-child.md -> W-00000002.md (planned work card)
  update backlog index (1 closed rows removed)
  stamp  settings.json schema_version = 4
Schema-v4 migration complete with no blocking audit findings. Guidance drift remains a non-blocking audit warning until the explicit refresh command is run.
All migration changes are staged; this command does not commit. Review them, then commit with: git commit -m "chore(stage): migrate project harness to schema v4"
Before committing, `migrate_stage.py --abort` restores the staged/working tree. After committing, rollback means `git revert <migration-commit>`.
Stage project already uses schema v4; no migration needed.
Preflight passed. Close every other agent/editor window before continuing; the schema-v4 maintenance marker now denies concurrent Stage writes.
  unchanged operations/verification.md (unchanged)
  delete backlog B-00000001-realized.md (realized by W-00000009; git history keeps the file)
  convert backlog B-00000002-open.md -> W-00000001.md (planned work card)
  convert backlog B-00000003-child.md -> W-00000002.md (planned work card)
  update backlog index (1 closed rows removed)
  stamp  settings.json schema_version = 4
Schema-v4 migration complete with no blocking audit findings. Guidance drift remains a non-blocking audit warning until the explicit refresh command is run.
All migration changes are staged; this command does not commit. Review them, then commit with: git commit -m "chore(stage): migrate project harness to schema v4"
Before committing, `migrate_stage.py --abort` restores the staged/working tree. After committing, rollback means `git revert <migration-commit>`.
----------------------------------------------------------------------
Ran 346 tests in 36.823s

OK

$ python3 -m unittest discover -s stage/hooks/tests -q
[exit 0]
----------------------------------------------------------------------
Ran 327 tests in 0.914s

OK

$ python3 stage/scripts/audit_stage.py --project-root .
[exit 0]
Stage audit: /Users/woogis/Workspace/repo/noory-ai/.stage
OK: no findings
Summary: errors=0, warnings=0
```

## Next action

`start_work.py` 로 시작한다. scope 는 그때 준다: `stage/skills/stage-retrospective/,
stage/skills/stage-drive/, stage/.claude-plugin/plugin.json, stage/.codex-plugin/plugin.json,
stage/CHANGELOG.md`. venue 는 documentation 정책대로 claude.

## Progress

### 계획에 더한 것 하나와 그 이유 (2026-07-26)

`stage-drive` 의 `NO-PROGRESS` 설명 문단이 지금은 틀린 사실을 말하고 있었다 — 지문이 새
파일과 스테이징된 변경을 못 본다고 적혀 있는데, 그것은 W-00000073 이전의 동작이고 지금
지문은 `HEAD` 대비 추적 diff 와 미추적 파일 해시를 다 본다. 처리 단계를 적으러 들어간 바로 그
자리의 문단이라, 틀린 채 두는 것이 더 큰 위반(Honesty)이라 함께 바로잡았다. 이 카드의 원래
목적 밖 변경이므로 여기 적어 근거를 남긴다. 같은 김에 W-00000083 이 만든 무변경 실패 규칙과
`close_work.py` 수동 경로도 그 문단에 들어갔다.

## Retrospective

R-00000085. 핵심: 항목 하나·물음 둘로 가볍게 넣었고, 오늘 세 카드에서 손으로 세 번 검증된
절차를 그대로 문장으로 옮겼다. 카드 목적 밖 수정 하나(낡은 문단 바로잡기)는 근거를 남기고
받았다.

## Promotion decision

official 로 올릴 산출물 없음(promotion: not_applicable). 규칙의 SSOT 는 승격된 DE-00000032.
카드와 회고는 아카이브로 간다.
