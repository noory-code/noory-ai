---
id: W-00000178
title: 드라이버가 승격 판정을 대신 찍지 못하게 한다
kind: development
venue: codex
milestone:
autonomous: false
acceptance:
  - "python3 -m unittest discover -s stage/scripts/tests -q -p test_drive.py"
status: active
verification: pending
retrospective: pending
retrospective_ref:
promotion: pending
review: not_required
scope: stage/scripts/drive.py, stage/scripts/audit_stage.py, stage/scripts/tests/, stage/operations/, stage/CHANGELOG.md
promotes:
decision_refs:
---

# W-00000178 드라이버가 승격 판정을 대신 찍지 못하게 한다

## Purpose

드라이버가 닫을 때 승격 값을 상수로 넘겨서 방금 세운 게이트를 통째로 지나간다.

## Source

W-00000177 의 판정이 찾았다. 게이트를 세우고 나서야 그 게이트에 닿는 길이 둘인 것이 드러났다.

## User value

드라이버로 돌린 일도 사람이 닫은 일과 같은 판정을 받는다. 이 저장소 일은 대부분 드라이버가
도므로, 여기가 안 막히면 게이트가 있으나 마나다.

## Scope

### Included

- 드라이버가 닫을 때 승격 값을 스스로 정하지 않게 한다. 카드가 든 값을 쓴다.
- `approved` 로 닫힌 카드가 실제로 승격했는지 감사가 본다.
- 두 갈래를 다 보는 시험.

### Excluded

- 무엇을 승격할지 자동으로 고르는 일. 판정은 사람이나 실행자가 한다.
- 닫는 명령의 게이트 자체. W-00000177 이 실었다.

## Dependencies

W-00000177 이 먼저다. 그 게이트가 있어야 이 카드가 막는 우회로가 뜻을 가진다.

## Risks

- **드라이버가 아무 카드도 못 닫게 되면 무인 실행이 죽는다.** 실행자가 카드에 승격 판정을
  적으면 통과해야 한다. W-00000176 에서 실제로 그렇게 돌았다.
- **감사에 검사를 더하면 옛 카드가 무더기로 걸릴 수 있다.** 보관된 카드 중 `approved` 인데
  승격 안 한 것이 몇 장인지 먼저 세고, 많으면 새 카드부터 걸게 한다.

## Success criteria

- 드라이버가 닫는 카드도 사람이 닫는 카드와 같은 승격 판정을 받는다.
- 실행자가 카드에 승격 판정을 적어 두면 드라이버가 그대로 닫는다.
- `approved` 로 닫혔는데 승격 안 한 카드를 감사가 잡는다.
- 사람이 겪는 결과: 드라이버로 돌렸다고 판정을 건너뛰는 길이 없다.

## Next action

`drive.py:1545` 가 `--promotion approved` 를 상수로 넘긴다. 그 자리를 카드가 든 값으로
바꾸고, 카드가 안 정했으면 닫기가 막히게 둔다. 그다음 보관된 카드에서 `approved` 인데
승격 안 한 것을 세고 감사 검사를 어디까지 걸지 정한다.

## Related truth

- **DE-00000030** — 일회성 허가는 대기에 남고 나머지 `decided` 는 승격한다.


## Progress


## Verification


## Retrospective


## Promotion decision
