---
id: W-00000075
title: 무인 모드가 기록 커밋 실패를 삼키고 부모 감사를 건너뛴다
kind: fix
venue: codex
parent:
source:
autonomous: false
acceptance: []
status: active
verification: pending
retrospective: pending
retrospective_ref:
promotion: pending
review: not_required
scope: stage/scripts/drive.py, stage/scripts/tests/, stage/docs/SCHEMA_V4.md, stage/.claude-plugin/plugin.json, stage/.codex-plugin/plugin.json, stage/CHANGELOG.md
promotes:
decision_refs:
---

# W-00000075 무인 모드가 기록 커밋 실패를 삼키고 부모 감사를 건너뛴다

## Purpose

무인 드라이버가 Stage 기록을 커밋하는 네 자리 중 셋에서 실패를 확인하지 않아, 커밋이 실패해도 성공한 것처럼 계속 진행한다. 그리고 부모를 닫을 때 부모에 acceptance가 선언돼 있으면 감사를 건너뛰어, 결정 기록 DE-00000027이 정한 검증 계약과 어긋난다.

## Scope

무인 드라이버(`drive.py --unattended`)의 결함 둘. W-00000070 의 독립 리뷰가 지적했고 코드에서
직접 확인했다.

### 1. 기록 커밋 실패를 삼킨다

무인 루프는 Stage 기록(카드 상태, 회고, 인덱스)을 격리 브랜치에 커밋한다. 그 커밋이 실패하면
병합했을 때 실행 결과만 있고 기록이 없다 — 누가 무엇을 왜 했는지가 사라진다.

네 자리 중 한 곳만 실패를 확인한다:

| 위치 | 무엇을 커밋 | 실패 확인 |
|---|---|---|
| `drive.py:614` | 에스컬레이션 기록 | **안 함** |
| `drive.py:750` | 재시도 전 기록 | **안 함** |
| `drive.py:756` | 항목 종료 기록 | 함 (`lc_ok` 검사 후 중단) |
| `drive.py:764` | 상위 집계 기록 | **안 함** |

756 줄이 올바른 형태다. 나머지 셋을 같은 방식으로 맞춘다. 커밋이 실패하면 계속 진행하지 않고
멈추고 사람에게 돌린다 — 기록 없는 진행은 완료가 아니다.

### 2. 부모를 닫을 때 감사를 건너뛴다

`close_ready_ancestors()` 의 `drive.py:557`:

```python
checks = list(parent.acceptance) or [audit_check(project_root)]
```

부모에 `acceptance` 가 선언돼 있으면 감사가 아예 돌지 않는다. 결정 기록
`official/decisions/records/DE-00000027.md` 는 부모의 검증 계약을 감사와 집계 게이트로 정하고
있다(자식은 이미 독립 리뷰를 거쳤으므로 부모는 그 둘로 검증한다). 부모가 자기 acceptance 를
선언했다는 이유로 감사가 사라지는 것은 그 계약과 어긋난다.

감사는 항상 돈다. 부모에 선언된 `acceptance` 가 있으면 **감사에 더해서** 돈다.

제약:

- **커밋하지 않는다.** 작업 트리에 변경만 남기고 멈춘다.
- 저장소 산출물은 영어로 쓴다. `.stage/` 안의 글만 한국어다.
- 두 `plugin.json` 의 version 을 올리고 `stage/CHANGELOG.md` 에 항목을 추가한다.
- scope 밖은 건드리지 않는다.

## Success criteria

- 세 자리(614, 750, 764)에서 기록 커밋이 실패하면 무인 실행이 멈추고 사람에게 돌아간다.
  성공한 것처럼 다음으로 넘어가지 않는다.
- 각 자리마다 커밋 실패를 흉내 내어 실행이 멈추는 것을 확인하는 테스트가 있다. 그 테스트는
  고치기 전 코드에서 실패해야 한다.
- 부모를 닫을 때 감사가 항상 돈다 — 부모에 `acceptance` 가 선언돼 있어도 마찬가지다.
- 부모에 `acceptance` 가 있는 경우 감사와 그 acceptance 가 **둘 다** 실행되는 것을 확인하는
  테스트가 있다. 그 테스트는 고치기 전 코드에서 실패해야 한다.
- `stage/docs/SCHEMA_V4.md` 의 무인 모드 절이 두 동작을 정확히 서술한다.
- `python3 -m unittest discover -s stage/scripts/tests -q` 통과.
- `python3 -m unittest discover -s stage/hooks/tests -q` 통과.

## Related truth

- 부모 검증 계약: `.stage/official/decisions/records/DE-00000027.md`.
- 무인 모드 사양: `stage/docs/SCHEMA_V4.md` — `### Unattended driver loop`.
- 발견 경위: W-00000070 의 2차 독립 리뷰.

**위험**: 이 둘은 무인 모드에서만 도는 경로다. 감독 모드로는 재현되지 않으므로, 기존
`test_drive_unattended.py` 의 준비 코드를 그대로 써야 한다. 새로 짜면 실제 브랜치를 남기거나
작업 트리를 더럽힌다. 그리고 커밋 실패를 흉내 낼 때 진짜 git 저장소를 망가뜨리지 말 것 —
기존 테스트가 쓰는 방식을 따를 것.

## Progress


## Verification


## Retrospective


## Promotion decision
