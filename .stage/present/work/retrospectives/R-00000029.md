---
id: R-00000029
work_item: W-00000030
---

# R-00000029 dormant v4 template tree (C3)

## Work

Added the schema-v4 template tree and TH-/M- roadmap templates under an isolated
stage/templates/v4/, with the ko overlay and five new tests. Ships dormant: init/hooks/
scanners untouched, init still produces a v3 .stage (verified). Executed by codex under the
cross-venue delegation contract; reviewed and committed by the claude host (9b256c67, 0.26.0).

## Decision points

- Structural improvement accepted: the v4 tree lives in its own stage/templates/v4/ directory
  rather than intermingled with the v3 project-stage/ tree. Rationale: init_stage.py copies the
  whole project-stage tree, so intermingling would have copied BOTH trees into new projects.
  The isolated dir keeps v4 fully inert until the C4 cutover repoints init.
- TH-/M- templates carry no authored status field (computed from the decision chain, per
  DE-00000009/SCHEMA_V4.md).

## Principles applied

- Fail Fast: an init smoke test proves dormancy (new projects still get past/, not official/).
- SSOT / parity: the ko overlay parity test was extended, not weakened.

## Context that helped

- SCHEMA_V4.md's explicit topology and roadmap-frontmatter sections made the mapping
  unambiguous.

## Context that was missing

- The card spec said "alongside the v3 tree"; the isolated-directory choice is better and was
  accepted at review. SCHEMA_V4.md's C3 row should note the v4 tree lives at templates/v4/ —
  folded into C4's context rather than reopening this card.

## Next changes

- C4 repoints init_stage.py (and rewires hooks/scanners/audit/context to the registry), staying
  dormant/schema-gated until the C7 activation release.

## Promotion decision

not_applicable — plugin source is canonical in the repository.
