---
id: W-00000001
title: Repo-wide documentation cleanup
kind: documentation
parent:
source:
status: active
verification: passed
retrospective: pending
retrospective_ref:
promotion: not_applicable
scope: CLAUDE.md, README.md, evonest/, stage/, plainly/, flutter-cask/
promotes:
decision_refs:
---

# W-00000001 Repo-wide documentation cleanup

## Purpose

User asked for a full documentation audit and cleanup across the monorepo (open-source
project, wanted a pass before it accumulates more drift). Audited root docs plus all six
packages for stale facts, SSOT duplication, evergreen violations, banned vague phrases,
broken links, and version/CHANGELOG drift.

## Scope

Root `README.md`/`CLAUDE.md`, and the `evonest`, `stage`, `plainly`, `flutter-cask` packages
(`rag` and `pencil_m3_flutter` were audited and needed no changes).

## Success criteria

Every finding from the audit pass is fixed, each touched package gets its version bumped and
a CHANGELOG entry, and every change lands as its own commit.

## Related truth

N/A — no `past/` canon changed by this work.

## Progress

- Root docs fixed and pushed (`412c0eff`).
- `plainly` fixed and pushed (`46f969d1`).
- `flutter-cask` fixed and pushed (`df557a75`) — also repaired a broken CI workflow.
- `stage` fixed and pushed (`e945074e`).
- `evonest` fixed and pushed (`d04140df`), plus a follow-up pass correcting a repo-wide
  stale data-root path (`.evonest/` → `.noory/evonest/`) and a resolved ROADMAP item.

This work item was registered retroactively after Stage governance was initialized
mid-task; the first five commits landed before `.stage/` existed.

## Verification

Each package's test suite (where present) was run after its edits; no regressions.
`stage/scripts/audit_stage.py` passes on this harness itself.

## Retrospective


## Promotion decision

Not applicable — this work touches only docs/CHANGELOG/version fields, nothing under
`.stage/past/`.
