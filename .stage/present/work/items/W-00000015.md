---
id: W-00000015
title: Complete the role-routing contract left unfinished by B-00000001..3
kind: fix
venue: claude
parent:
source:
status: active
verification: pending
retrospective: pending
retrospective_ref:
promotion: pending
scope: stage, .stage
promotes: .stage/past/canon/vocabulary.md
decision_refs: DE-00000005, DE-00000006
---

# W-00000015 Complete the role-routing contract left unfinished by B-00000001..3

## Purpose

Close the review-found gaps so the routing goal actually holds — Claude takes design, Codex takes
implementation, mixed work splits with lineage, and exceptions apply only through approved
decisions — with behavior, audit, and tests changed together, not documents alone.

## Scope

- Templates (English and Korean) and this repository's canon: `settings.json` `venue_routing` is
  the machine routing SSOT; `vocabulary.md` owns only venue meaning; remove "advisory" claims
  that contradict audit behavior.
- A machine-readable split contract for mixed kinds: registration fails fast with the exact
  resolution action; a decision-linked exception permits a deliberate single item; no semantic
  guessing anywhere.
- Venue exceptions: the registration CLI refuses an unapproved policy-contradicting venue; the
  CLI and the audit validate the same contract (linked decision exists, names the work item, is
  `decided`/`promoted`, and machine-readably authorizes a venue exception).
- Completion gate: a work item cannot complete while any linked decision is still `open`; the
  audit reports completed/archived items referencing open decisions; DE-00000002..4 statuses
  corrected.
- This repository declares `"language": "en"` explicitly so records and settings agree; routing
  covers `feature`, `documentation`, and `chore`.
- Failing tests first; both manifests bumped; changelog updated.

## Success criteria

- Fresh English and Korean initializations point to `settings.json` as the routing SSOT and to
  canon for meaning only.
- Registering a split-marked kind as one item fails with the split instruction unless a valid
  exception decision is linked; routed kinds derive venue without asking; no-policy,
  single-venue, and custom-venue projects keep current behavior.
- An unapproved policy-contradicting venue is refused at registration and reported by the audit
  under one shared validation.
- A completed item linked to an `open` decision is an audit error and cannot close.
- `python3 -m unittest discover -s stage/hooks/tests -q`,
  `python3 -m unittest discover -s stage/scripts/tests -q`,
  `python3 stage/scripts/audit_stage.py --project-root . --format json` (errors=0), and
  `git diff --check` all pass.

## Related truth

- Completes the residue of `B-00000001..3` (W-00000012..14).
- `DE-00000005` owns the split/exception/completion contract; `DE-00000006` authorizes this
  item's own venue exception.

## Progress

-

## Verification

-

## Retrospective

-

## Promotion decision

-
