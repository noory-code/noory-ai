---
id: W-00000017
title: Implement the unified work-card lifecycle (kanban moves, B family retires)
kind: development
venue: claude
parent: W-00000016
source:
status: completed
verification: passed
retrospective: completed
retrospective_ref: R-00000017
promotion: not_applicable
review: not_required
scope: stage, .stage
promotes:
decision_refs: DE-00000008
---

# W-00000017 Implement the unified work-card lifecycle (kanban moves, B family retires)

## Purpose

W cards live in future/backlog and move to present on start; migration v3 converts B items.

## Scope

- Planned statuses in the shared enum; audit checks for column/status consistency, cross-column
  parents, index lock-step, and id uniqueness across columns.
- `register_work.py --backlog` capture; `scripts/start_work.py` move with scope requirement and
  the venue/split contract at start.
- Templates (en/ko), plugin backlog/artifacts operation docs, skills, context injection.
- Migration v3 (B conversion/removal) and this repository's own migration.

## Success criteria

- A card keeps one id from capture to archive; hand-moving is unnecessary; audits stay clean for
  new, legacy (un-migrated), and migrated projects.
- Both test suites pass; `audit_stage.py --format json` errors=0; `git diff --check` clean.

## Related truth

- Implements `DE-00000007` under parent `W-00000016`; venue exception per `DE-00000008`.

## Progress

- Shared planned-status enum shipped in `stage_work.py`; audit rewritten (BACKLOG001/002/003
  against W cards, retired WORK020/WORK023/BACKLOG004/005/006, dual-prefix index membership,
  cross-column SSOT001).
- `--backlog` capture and `start_work.py` mover shipped with 9 new flow tests and 2 migration
  tests (172 scripts tests total).
- Templates and docs updated in both languages under the parity test; this repository migrated
  to schema 3 with B-00000001..3 drained.
- Released as 0.23.0.

## Verification

Executed this session:

```
$ python3 -m unittest discover -s stage/hooks/tests -q
[exit 0]
----------------------------------------------------------------------
Ran 269 tests in 0.484s

OK

$ python3 -m unittest discover -s stage/scripts/tests -q
[exit 0]
  unchanged operations/verification.md (unchanged)
  delete backlog B-00000001-realized.md (realized by W-00000009; git history keeps the file)
  convert backlog B-00000002-open.md -> W-00000001.md (planned work card)
  convert backlog B-00000003-child.md -> W-00000002.md (planned work card)
  update backlog index (1 closed rows removed)
  stamp  settings.json schema_version = 3
Migration complete.
  unchanged operations/verification.md (unchanged)
Migration complete.
  unchanged operations/verification.md (unchanged)
  delete backlog B-00000001-realized.md (realized by W-00000009; git history keeps the file)
  convert backlog B-00000002-open.md -> W-00000001.md (planned work card)
  convert backlog B-00000003-child.md -> W-00000002.md (planned work card)
  update backlog index (1 closed rows removed)
  stamp  settings.json schema_version = 3
Migration complete.
----------------------------------------------------------------------
Ran 172 tests in 5.201s

OK
```

## Retrospective

See `R-00000017` in `present/work/retrospectives/`.

## Promotion decision

`promotion: not_applicable` — all changes live in the `stage/` plugin and this repository's `.stage/` configuration.
