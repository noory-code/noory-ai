"""Pure domain models and ports (interfaces) for rag.

This package is dependency-free at the framework level: only the standard
library and ``numpy`` are imported. Concrete adapters live in
``rag_mcp.infrastructure`` and must satisfy the Protocols defined here.
"""
