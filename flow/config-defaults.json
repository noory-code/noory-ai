{
  "_comment": "Plugin-internal default-settings SSOT. flow-config (initial fill) and flow-upgrade (seed-if-absent) inject these values into the project's .flow/settings.json. The upstream_board coordinates below are a placeholder — set your own GitHub Project (owner + number) before publishing retrospective backlog tickets. Projects that use a different board override this in settings.",
  "upstream_board": {
    "type": "github-project",
    "owner": "noory-code",
    "number": 0,
    "url": "https://github.com/orgs/noory-code/projects/0"
  },
  "skill_usage": { "enabled": true }
}
