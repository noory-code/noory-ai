# Proposal Archive

This directory owns proposals that have reached a recorded outcome.

## Rules

- Folder placement is the lifecycle state; archived records do not add a status field.
- Each proposal keeps its `accepted`, `rejected`, or `partial` outcome in its record and archive
  index.
- `index.md` lists the records in this archive.
